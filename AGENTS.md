# AGENTS.md

## Project Workflow

- Do not run visual QA, browser renders, screenshots, or manual UI testing unless the user explicitly asks for it.
- For UI prototypes, implement the requested changes and let the user check the result directly in the browser.
- After UI edits, report the changed files and any important assumptions instead of producing render artifacts.
